fastapi>=0.104
uvicorn[standard]>=0.24
twilio>=8.10
httpx>=0.25
python-dotenv>=1.0

# ── Database (async SQLAlchemy) ───────────────────────────────────
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.19          # SQLite async driver (local dev)
asyncpg>=0.29            # PostgreSQL async driver (production)
psycopg2-binary>=2.9     # PostgreSQL sync driver (migrations)

# ── AWS SDK ───────────────────────────────────────────────────────
boto3>=1.34

# ── PDF processing (existing) ────────────────────────────────────
pdfplumber>=0.10
